[pytest]
testpaths = tests
addopts = --ignore=tests/test_performance.py -q -m "not slow"
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    performance: marks tests as performance tests
